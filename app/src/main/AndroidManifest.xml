<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">
    <!-- Permissions -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.INTERNET" />
    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.G4PrmLibrary"
        tools:targetApi="31">
        <!-- Login Activity - LAUNCHER -->
        <!-- Login Activity - LAUNCHER -->
        <activity
            android:name="com.example.g4prmlibrary.authentication.LoginActivity"
            android:exported="true"
            android:screenOrientation="portrait">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Main Activity -->
        <activity
            android:name=".MainActivity"
            android:exported="false"/>


        <!-- Register Activity -->
        <activity
            android:name="com.example.g4prmlibrary.authentication.RegisterActivity"
            android:exported="false"/>

        <!-- Forgot Password Activity -->
        <activity
            android:name="com.example.g4prmlibrary.authentication.ForgotPasswordActivity"
            android:exported="false"/>


        <activity
            android:name="com.example.g4prmlibrary.authentication.ChangePasswordActivity"
            android:exported="false"/>
    </application>

</manifest>